{
  "name": "taskflow",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend && npm run db:generate --workspace=backend",
    "start": "npm start --workspace=backend"
  }
}
